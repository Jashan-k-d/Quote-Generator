<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Random Quote Generator</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #74ebd5, #9face6);
            font-family: 'Segoe UI', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .card {
            border: none;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
            background: rgb(255, 255, 255);
        }

        .quote {
            font-size: 1.6rem;
            font-style: italic;
            color: #333;
        }

        .author {
            font-size: 1.2rem;
            color: #555;
        }

        .btn-custom {
            background-color: #6f42c1;
            color: white;
            border-radius: 30px;
            padding: 10px 25px;
        }

        .btn-custom:hover {
            background-color: #5a33a3;
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card text-center p-4">
                    <div class="card-body">
                        <p class="quote mb-4">"<%= quotelist.quotes[num].quote %>"</p>
                        <p class="author">— <%= quotelist.quotes[num].author %>
                        </p>
                        <a href="/" class="btn btn-custom mt-4">Get Another Quote</a>
                    </div>
                </div>
            </div>
        </div>
    </div>


</body>

</html>